# Options to start crond with.
# If you want cron to actually log activity to /var/log/cron, then
# uncomment this, and change -l notice to -l info to increase 
# the logging level.
#CROND_OPTS="-l notice"
